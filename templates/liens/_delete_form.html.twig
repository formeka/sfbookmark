<form method="post" action="{{ path('app_liens_delete', {'id': lien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lien.id) }}">
    <button class="btn">Delete</button>
</form>
